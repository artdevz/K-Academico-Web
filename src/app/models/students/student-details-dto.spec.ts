import { StudentDetailsDTO } from './student-details-dto';

describe('StudentDetailsDTO', () => {
  it('should create an instance', () => {
    expect(new StudentDetailsDTO()).toBeTruthy();
  });
});
