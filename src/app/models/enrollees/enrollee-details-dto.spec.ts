import { EnrolleeDetailsDTO } from './enrollee-details-dto';

describe('EnrolleeDetailsDTO', () => {
  it('should create an instance', () => {
    expect(new EnrolleeDetailsDTO()).toBeTruthy();
  });
});
